import AddressFC from '@/components/address/Address';
import { NextPage } from 'next';

const Address: NextPage = () => {
  return (
    <div className="container">
      <AddressFC />
    </div>
  );
};

export default Address;
